# Secrets — NEVER commit these
.env
.env.local
.env.*.local

# Node
node_modules/
npm-debug.log*

# OS / editor
.DS_Store
Thumbs.db

# Local dev / preview tooling — not part of the deployed app
server.js
recap-test.html
mock-*.html
.claude/

# Throwaway drag-drop bundle (regenerated from index.html + _headers)
deploy/
